// Live panel: source control, frame grid, live signals, live trend, trace,
// recording. (Implemented in the live phase — this stub keeps the app bootable.)

export function initLivePanel(app) {
  // implemented in a later phase
}
