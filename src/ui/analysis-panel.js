// Analysis panel: signal tree, value table with update interval, playback,
// trend/XY graph stack with synchronized cursors.
// (Implemented in the analysis phase — this stub keeps the app bootable.)

export function initAnalysisPanel(app) {
  // implemented in a later phase
}
